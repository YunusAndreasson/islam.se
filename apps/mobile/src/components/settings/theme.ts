// Shared visual tokens for the settings screen. Kept tiny and local — the app
// has no design system yet, so these are the single source for settings colours.
export const colors = {
  bg: '#f2f2f7',
  card: '#ffffff',
  border: '#e3e3e8',
  text: '#11181c',
  textMuted: '#6b7280',
  // Same accent as the map chrome (solar night-indigo) so the whole app is one
  // palette — see components/map/theme.ts and lib/solar/palette.ts.
  accent: '#46527f',
  accentSoft: '#e9ebf5',
  separator: '#ececf0',
};
