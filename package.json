{
	"name": "islam-se",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "pnpm -r build",
		"check": "biome check .",
		"check:fix": "biome check --write .",
		"lint:articles": "markdownlint-cli2 'data/articles/*.md'",
		"knip": "knip",
		"test": "vitest run",
		"test:watch": "vitest",
		"cli": "pnpm --filter @islam-se/cli run cli",
		"produce": "pnpm --filter @islam-se/content-producer run produce",
		"tui": "ISLAM_OUTPUT_DIR=$(pwd)/apps/content-producer/output pnpm --filter @islam-se/tui run dev",
		"ship": "pnpm build && wrangler pages deploy apps/web/dist --project-name islam-se"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.6",
		"@types/node": "^25.3.5",
		"knip": "^5.85.0",
		"markdownlint-cli2": "^0.20.0",
		"typescript": "^5.9.3",
		"vitest": "^4.0.18",
		"wrangler": "^4.71.0"
	},
	"packageManager": "pnpm@9.15.0"
}
